# Backend environment variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# PostgreSQL connection string
# Format: postgresql://USERNAME@HOST:PORT/DATABASE
# Replace USERNAME with your OS username (run: psql -c "SELECT current_user;")
DATABASE_URL=postgresql://YOUR_USERNAME@localhost:5432/kaasu

# Optional: restrict CORS to specific origins in production
# Comma-separated list of allowed origins (defaults to http://localhost:3000)
# ALLOWED_ORIGINS=https://yourdomain.com
